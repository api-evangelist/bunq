naftiko: 1.0.0-alpha2
info:
  label: Bunq payment-service-provider-credential/ — Payment Service Provider Credential
  description: 'Bunq payment-service-provider-credential/ — Payment Service Provider Credential. 1 operations. Lead operation:
    Payment Service Provider Credential. Self-contained Naftiko capability covering one Bunq business surface.'
  tags:
  - Bunq
  - Payment Service Provider Credential
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    BUNQ_API_KEY: BUNQ_API_KEY
capability:
  consumes:
  - type: http
    namespace: payment-service-provider-credential--payment-service-provider-credential
    baseUri: https://public-api.sandbox.bunq.com/{basePath}
    description: Bunq payment-service-provider-credential/ — Payment Service Provider Credential business capability. Self-contained,
      no shared references.
    resources:
    - name: payment-service-provider-credential-itemId
      path: /payment-service-provider-credential/{itemId}
      operations:
      - name: readpaymentserviceprovidercredential
        method: GET
        description: Register a Payment Service Provider and provide credentials
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: itemId
          in: path
          type: integer
          required: true
  exposes:
  - type: rest
    namespace: payment-service-provider-credential--payment-service-provider-credential-rest
    port: 8080
    description: REST adapter for Bunq payment-service-provider-credential/ — Payment Service Provider Credential. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/payment-service-provider-credential/{itemid}
      name: payment-service-provider-credential-itemid
      description: REST surface for payment-service-provider-credential-itemId.
      operations:
      - method: GET
        name: readpaymentserviceprovidercredential
        description: Register a Payment Service Provider and provide credentials
        call: payment-service-provider-credential--payment-service-provider-credential.readpaymentserviceprovidercredential
        with:
          itemId: rest.itemId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: payment-service-provider-credential--payment-service-provider-credential-mcp
    port: 9090
    transport: http
    description: MCP adapter for Bunq payment-service-provider-credential/ — Payment Service Provider Credential. One tool
      per consumed operation, routed inline through this capability's consumes block.
    tools:
    - name: register-payment-service-provider-and
      description: Register a Payment Service Provider and provide credentials
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: payment-service-provider-credential--payment-service-provider-credential.readpaymentserviceprovidercredential
      with:
        itemId: tools.itemId
      outputParameters:
      - type: object
        mapping: $.
