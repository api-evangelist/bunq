naftiko: 1.0.0-alpha2
info:
  label: bunq API — Registry Import Splitwise Csv
  description: 'bunq API — Registry Import Splitwise Csv. 1 operations. Lead operation: Registry Import Splitwise Csv. Self-contained
    Naftiko capability covering one Bunq business surface.'
  tags:
  - Bunq
  - Registry Import Splitwise Csv
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    BUNQ_API_KEY: BUNQ_API_KEY
capability:
  consumes:
  - type: http
    namespace: bunq-registry-import-splitwise-csv
    baseUri: https://public-api.sandbox.bunq.com/{basePath}
    description: bunq API — Registry Import Splitwise Csv business capability. Self-contained, no shared references.
    resources:
    - name: registry-import-splitwise-csv
      path: /registry-import-splitwise-csv
      operations:
      - name: createregistryimportsplitwisecsv
        method: POST
        description: Used to upload a CSV export from Splitwise, and create a new Registry from it.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
  exposes:
  - type: rest
    namespace: bunq-registry-import-splitwise-csv-rest
    port: 8080
    description: REST adapter for bunq API — Registry Import Splitwise Csv. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/registry-import-splitwise-csv
      name: registry-import-splitwise-csv
      description: REST surface for registry-import-splitwise-csv.
      operations:
      - method: POST
        name: createregistryimportsplitwisecsv
        description: Used to upload a CSV export from Splitwise, and create a new Registry from it.
        call: bunq-registry-import-splitwise-csv.createregistryimportsplitwisecsv
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: bunq-registry-import-splitwise-csv-mcp
    port: 9090
    transport: http
    description: MCP adapter for bunq API — Registry Import Splitwise Csv. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: used-upload-csv-export-splitwise
      description: Used to upload a CSV export from Splitwise, and create a new Registry from it.
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: bunq-registry-import-splitwise-csv.createregistryimportsplitwisecsv
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
