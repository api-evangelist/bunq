naftiko: 1.0.0-alpha2
info:
  label: Bunq user/{userID}/tree-progress — User
  description: 'Bunq user/{userID}/tree-progress — User. 1 operations. Lead operation: User. Self-contained Naftiko capability
    covering one Bunq business surface.'
  tags:
  - Bunq
  - User
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    BUNQ_API_KEY: BUNQ_API_KEY
capability:
  consumes:
  - type: http
    namespace: user-userid-tree-progress-user
    baseUri: https://public-api.sandbox.bunq.com/{basePath}
    description: Bunq user/{userID}/tree-progress — User business capability. Self-contained, no shared references.
    resources:
    - name: user-userID-tree-progress
      path: /user/{userID}/tree-progress
      operations:
      - name: listalltreeprogressforuser
        method: GET
        description: See how many trees this user has planted.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: userID
          in: path
          type: integer
          required: true
  exposes:
  - type: rest
    namespace: user-userid-tree-progress-user-rest
    port: 8080
    description: REST adapter for Bunq user/{userID}/tree-progress — User. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/user/{userid}/tree-progress
      name: user-userid-tree-progress
      description: REST surface for user-userID-tree-progress.
      operations:
      - method: GET
        name: listalltreeprogressforuser
        description: See how many trees this user has planted.
        call: user-userid-tree-progress-user.listalltreeprogressforuser
        with:
          userID: rest.userID
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: user-userid-tree-progress-user-mcp
    port: 9090
    transport: http
    description: MCP adapter for Bunq user/{userID}/tree-progress — User. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: see-how-many-trees-this
      description: See how many trees this user has planted.
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: user-userid-tree-progress-user.listalltreeprogressforuser
      with:
        userID: tools.userID
      outputParameters:
      - type: object
        mapping: $.
