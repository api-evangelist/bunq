naftiko: 1.0.0-alpha2
info:
  label: Bunq user/{userID}/payment-auto-allocate — User
  description: 'Bunq user/{userID}/payment-auto-allocate — User. 1 operations. Lead operation: User. Self-contained Naftiko
    capability covering one Bunq business surface.'
  tags:
  - Bunq
  - User
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    BUNQ_API_KEY: BUNQ_API_KEY
capability:
  consumes:
  - type: http
    namespace: user-userid-payment-auto-allocate-user
    baseUri: https://public-api.sandbox.bunq.com/{basePath}
    description: Bunq user/{userID}/payment-auto-allocate — User business capability. Self-contained, no shared references.
    resources:
    - name: user-userID-payment-auto-allocate
      path: /user/{userID}/payment-auto-allocate
      operations:
      - name: listallpaymentautoallocateforuser
        method: GET
        description: List a users automatic payment auto allocated settings.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: userID
          in: path
          type: integer
          required: true
  exposes:
  - type: rest
    namespace: user-userid-payment-auto-allocate-user-rest
    port: 8080
    description: REST adapter for Bunq user/{userID}/payment-auto-allocate — User. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/user/{userid}/payment-auto-allocate
      name: user-userid-payment-auto-allocate
      description: REST surface for user-userID-payment-auto-allocate.
      operations:
      - method: GET
        name: listallpaymentautoallocateforuser
        description: List a users automatic payment auto allocated settings.
        call: user-userid-payment-auto-allocate-user.listallpaymentautoallocateforuser
        with:
          userID: rest.userID
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: user-userid-payment-auto-allocate-user-mcp
    port: 9090
    transport: http
    description: MCP adapter for Bunq user/{userID}/payment-auto-allocate — User. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: list-users-automatic-payment-auto
      description: List a users automatic payment auto allocated settings.
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: user-userid-payment-auto-allocate-user.listallpaymentautoallocateforuser
      with:
        userID: tools.userID
      outputParameters:
      - type: object
        mapping: $.
